name: kids_learning_app

description: A Flutter app for kids learning

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  shared_preferences: ^2.0.0

flutter:
  uses-material-design: true